#include <stdlib.h>
#include <stdio.h>
#include <time.h>

#define max 45

int main(void){

  int i;
  srand((unsigned)time(null));
  
  for( i =0; i <6; i++)
        printf("%d  ", 1+(rand()%MAX));
  
  printf("\n");
  return 0;
  }
